# 3200-group-project

## AI acknowledgement
login.css was generated by AI with prompt "generate a login page"

database entries 2-7 were genrated based on prompt "using entry 1 make more users" 

Explanation:
Run this file via VScode Terminal and type:
pip install cryptography (if this doesnt work type: python -m pip install cryptography)
python authentication.py
You want to choose option 1 -> which will allow you to enter your own credentials **Do not forget to delete them from the database after**. 
Option 2 should provide you with the broken database. 
Options 3 attacks it. You will see that it outputs the passwords in plaintext which is bad. **This is the broken example**.
Option 4 creates a new file **Do not forget to delete this too after the demo**
Option 5 Shows the secure database with the hashed passwords and the salts.
Option 6 simulates a dictionary attacks and shows that the passwords are compromised.
Option 7 allows you to login.
Option 8 RSA needs more work. So far, all it does is it has a random text and it turns it into hash. I was thinking we could change this to where upon login in (in steps 1-2) you are able to type text into your account, and then upon secure login in step 7, you are able to view your text in RSA. 
Option 9 is Exit. Please delete all the files and database entries you make and make sure you follow each step.
